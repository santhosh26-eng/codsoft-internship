import random

def generate(length):
  password = ""
  for i in range(length):
    password += random.choice("ABCDEFGHIJKL12345678qwertyu")
  return password

length = int(input("Enter the  length: "))
password = generate(length)
print("  The password is", password)
